
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p0@rails3tutorial" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p0@rails3tutorial"
else
  rvm --create use  "ruby-1.9.2-p0@rails3tutorial"
fi
